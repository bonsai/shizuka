# Work Log — 2026-06-21

**エージェント**: Kilo
**プロジェクト**: Model Manager
**開始**: 08:02 JST

---

## Recap 1 (08:02-08:12)

### 作業内容
- MISSION.md 作成 (使命・原則・憲章)
- KANBAN.md 作成 (4 Phase, 25 Issues)
- ADR-005 最終決定 (BigQuery + Secret Manager)
- 作業日誌システム構築 (`model-manager/log/`)

### 課題
- BigQuery DDL を SQLite の schema.sql から移行する必要あり（データ型・インデックス構文の違い）
- gcloud CLI が利用可能か未確認

### ナイスアイデア
- 作業ログを Markdown で取ることで、全エージェントから参照可能・git管理も可能
- log ファイルを日付+連番で運用: `YYMMDD-NNN.md`

### 発見
- 作業の可視化には KANBAN.md が有効。進捗が一目でわかる
- 次の着手は BigQuery DDL (Phase 2 #8)

### 引き継ぎ
- なし（継続中）

---

## Recap 2 (08:12-08:22)

### 作業内容
- `bq_ddl.sql` 作成 (BigQuery 版 DDL)
- `gcloud bq mk dataset` — dataset `model_status` 作成成功
- 全7テーブル + 1ビュー作成完了
- `models.agents.md` の YAML から seed データを抽出し BQ に投入
- 8モデルの seed 成功確認

### 課題
- BigQuery は DEFAULT 制約非対応 → 全て nullable + アプリ側でデフォルト処理
- PowerShell の `<` リダイレクト非対応 → `Get-Content | bq query` で回避
- NDJSON 形式でないと BQ load できない（配列JSON はエラー）

### ナイスアイデア
- `models.agents.md` の YAML frontmatter を source of truth にすることで、BQ seed も自動生成可能
- BQ の CLUSTER BY で providers の priority 順スキャンを最適化

### 発見
- BQ DDL は SQLite と結構違う (DEFAULT 不可, AUTOINCREMENT 不可, BOOL 型, PARTITION BY)
- `bq load --replace` で冪等な seed 投入可能

### 引き継ぎ
```
KANBAN #8: ✅ DONE (BigQuery dataset + DDL)
次: KANBAN #9 — selector.py BQ バックエンド実装
```

---

## Recap 3 (08:22-08:32)

### 作業内容
- (ここに追記)

### 課題
-

### ナイスアイデア
-

### 発見
-

### 引き継ぎ
-
